# Add the existing external Cloudflare network:
# docker compose -f compose.yaml -f compose.cf-net.yaml up -d --build
services:
  iptv-proxy:
    networks:
      - default
      - cf-net

networks:
  cf-net:
    external: true
    name: cf-net
